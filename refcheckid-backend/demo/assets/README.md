# ARCH-1 Demo Photo Assets

This directory is reserved for committed demo images used by the ARCH-1 Demo Bootstrap.

Milestone 2 defines deterministic asset paths in `../arch1-demo-manifest.json`; later milestones will add or generate the concrete images and submit them through Upload Intent and Upload Complete. Files in this directory must never be copied directly into runtime photo storage.
